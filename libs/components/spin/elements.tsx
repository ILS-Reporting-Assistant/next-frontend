import styled from 'styled-components'
import { BoxProps } from '../../types'
import { Box } from '../box'

export const StyledSpin = styled((props: BoxProps) => <Box {...props} />)`
  /* Add any custom styling for Spin component here */
`
